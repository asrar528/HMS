# =============================================================
# HMS – Local development environment variables
# Copy this file to .env and fill in your values.
# NEVER commit the real .env file to version control.
# =============================================================

# ── Flask core ───────────────────────────────────────────────
# Generate a strong key with:  python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-to-a-long-random-string

# Set to "production" on the cloud server, leave as "development" locally
FLASK_ENV=development

# ── Server ───────────────────────────────────────────────────
# Port the dev server listens on (cloud platforms set this automatically)
PORT=5000

# ── Database ─────────────────────────────────────────────────
# Absolute path to the folder that holds hms.db
# On Render: set to the mountpoint of your persistent Disk, e.g. /data
# On Railway: leave unset (ephemeral) or add a PostgreSQL plugin instead
# DATA_DIR=/data
